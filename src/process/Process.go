package process

import (
	"config"
)

func Run(sc *config.SystemConfig, yc *config.YamlConfig) {
	//vcs.PrepareVCS(sc, yc)
	//build.Build()

	//TODO build deploy image
	//

	//TODO push to repo
	//push name.. tag..
	//cli.ImagePush(context.Background(), []string{"", ""})
}
